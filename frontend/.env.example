# frontend/.env.example
# Copy to .env.local for local development (Vite reads .env.local automatically)
# IMPORTANT: variables must start with VITE_ to be accessible in the browser

# URL of the Laravel API
# In development (Docker): nginx proxies /api/* to php-fpm, so localhost works
VITE_API_URL=http://localhost/api/v1
